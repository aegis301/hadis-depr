from django.conf import settings

# if mongo is selected use mongo populator

# check settings again, there is only the postgresql database configurations, I might have to add djongo again

# check installed_apps for the same reason
def run():
    print("Hello World")

if __name__ == '__main__':
    run()